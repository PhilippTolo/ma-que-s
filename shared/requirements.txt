# Core training
torch>=2.3.0
transformers>=4.51.0
datasets>=2.19.0
accelerate>=0.30.0
trl>=0.9.0          # SFT + GRPO trainers
peft>=0.11.0        # LoRA (optional, saves VRAM)

# Inference / CI validation
vllm>=0.4.3

# Evaluation / utils
langdetect>=1.0.9   # language-consistency reward for multilinguality
rouge-score>=0.1.2
evaluate>=0.4.0
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=1.26.0
tqdm>=4.66.0

# HuggingFace
huggingface_hub>=0.23.0
